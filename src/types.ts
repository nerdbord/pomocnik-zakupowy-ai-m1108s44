export type NAV_ITEM = {
  id: string;
  label: string;
  href: string;
};
[];

export type FEATURE_ITEM = {
  id: number;
  title: string;
  description: string;
};

export type FAQ_ITEM = {
  id: number;
  answer: string;
  question: string;
};
