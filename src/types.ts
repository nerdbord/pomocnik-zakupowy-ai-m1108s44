export type NavItem = {
  id: string;
  label: string;
  href: string;
};

export type FeatureItem = {
  id: number;
  title: string;
  description: string;
};

export type FaqItem = {
  id: number;
  answer: string;
  question: string;
};